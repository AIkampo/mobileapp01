

import 'package:flutter/animation.dart';

const animationPageMilliseconds = 300;

const animationPageDuration = Duration(milliseconds: animationPageMilliseconds);

const animationPageCurve = Curves.easeInOut;